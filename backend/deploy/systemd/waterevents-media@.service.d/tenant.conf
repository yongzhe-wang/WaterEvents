[Service]
# The media fleet's half of the tenant identity. See waterevents-worker@.service.d/tenant.conf for why this is an
# EnvironmentFile rather than an Environment= line.
EnvironmentFile=/etc/waterevents/tenant-media.env
