# /etc/waterevents.env — mode 600, root-owned. systemd EnvironmentFile: literal values, NO shell expansion.
# Mirrors the exports in launch_fleet.sh so the units and the script stay behaviourally identical.
WATEREVENTS_DB_SCHEMA=waterevents
WATEREVENTS_RUN_ID=eventinc
QWEN_BASE_URLS=http://127.0.0.1:8000/v1
QWEN_SERVED_NAME=qwen-vl
WATERCRAWL_NO_SHOT=1
EVENT_USE_IMAGE=0
EVENT_MAX_PAGES=30
EVENT_BATCH=5
EVENT_COMPANY_BUDGET_S=600
EVENTINC_TOP_K=3
EVENTINC_PROFILE=runpod
PYTHONPATH=/home/thebigsun/WaterEvents/backend
# secrets appended at install time (DSN / QWEN_API_KEY / WEBSHARE_PROXY) — never committed
