# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/
*.egg-info/

# OS
.DS_Store

# Runtime / logs (live on the VM, never in git)
*.log
qwen_logs/

# Model weights / checkpoints — live on the VM (/mnt/data/hf_cache) or GCP, NEVER in git or on the Mac
*.safetensors
*.bin
*.gguf
*.pt
*.pth
hf_cache/

# Test RUN OUTPUT (regenerable — harnesses recreate; reusable datasets live in tests/datasets/ and ARE tracked)
tests/output/
tests/output_full/
tests/media_output/
tests/media_results/
tests/10event/
tests/datasets/ir_pdfs/results/
tests/datasets/ir_pdfs/results.json
tests/**/traces/
tests/**/_dbg_*/
tests/**/_trace_*/
# run-output dirs (regenerable traces/screenshots/HTML/JSONL — never in git; the .py harnesses ARE tracked)
tests/*_out/
tests/thekillerdeal/
tests/ir_official_variants/
tests/stress1000/
tests/stress_txt/
tests/par3/
tests/edge_review/
tests/lnn_out/
tests/eval_set.jsonl
tests/hstress_dataset.jsonl

# Secrets
.env
*.key

# build + deps (never commit)
node_modules/
dist/
.vercel/
*.tsbuildinfo
frontend/node_modules/
frontend/dist/
frontend/.vercel/

# large binary test fixtures (keep scripts, not the PDFs)
tests/datasets/ir_pdfs/
tests/**/*.pdf

# macOS AppleDouble resource forks. Not created by any tool in this repo — they appear when a Mac
# tars/rsyncs into the Linux VM (bsdtar writes ._<name> siblings carrying xattrs). 196 of them
# accumulated under backend/ and tests/ that way. Inert (Python never imports a `._x.py`), but they
# pollute grep/find and make a directory listing unreadable. Set COPYFILE_DISABLE=1 when tarring
# from macOS to stop producing them at the source. {CLEANED 2026-08-03 196 files / 784K on ir-media-8}
._*
.AppleDouble
